# Verification dependencies. Runtime generators still use only the standard library.
# Browser acceptance tests (#7, #9, #10, #11).
playwright==1.61.0
# Validating the correction form against GitHub's issue-form schema (check T7). Without it that
# check reports INFO instead of passing silently.
PyYAML==6.0.2
