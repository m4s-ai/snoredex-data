<!-- doc: role=issue-258 Indonesian physical evidence; stage=reference -->
# Indonesian AC3b 238/204 seller photograph — 2026-09-24

SPEC-0557 retains the exact Shopee seller photograph supplied for review: Indonesian Eevee & Snorlax GX, AC3b C 238/204 SR. The card face shows Indonesian text, the exact collector number and rarity, and reflective foil across the face and border. The observation records `holo`; it does not assign a specific foil pattern or close the finish inventory.

The original 1024×1024 JPEG is retained and imported unchanged. Its SHA-256, byte size, MIME type, dimensions and provenance are recorded in `sources.json`.

The Shopee listing title identifies the item as an Indonesian AC3b 238/204 SR, but the card face is the basis for identity and visible finish. Tesseract is unavailable in this environment, so text identification is by visual reading. The image depicts one seller-held copy. SPEC-0557 remains an unprojected candidate because no matching canonical source-first release or finish unit is present; it is not linked to SPEC-0335, which represents a separate collector-carousel image without retained bytes. No same-copy relationship, independent corroboration or complete finish inventory is asserted. The listing page presented a CAPTCHA; it was not interacted with. The separately supplied original photo URL was reachable and is retained as the image source.

## Acceptance

Accepted by `verification/fetch_attachment.py --manifest`: 1024×1024 JPEG prepared; one specimen imported; photo integrity passed (467 photographed specimens, offline). The physical workflow loop completed one cycle and stopped at `no-metric-change` (`20260924T180847Z-physical`). Scoped physical-evidence regeneration passed all 5 checks with 0 skipped or failed (`20260924T180908Z-physical-evidence-5dba233830`). `verification/review_findings.py` passed 111/111 checks. Full `scripts/regen.py` refreshed projections; its only failures were the known Windows temporary-directory permission errors in the database and tracker checks. The candidate remains unprojected until a canonical release is admitted.
